:root {
  --bg: #0f172a;         /* slate-900 */
  --panel: #111827;      /* gray-900 */
  --text: #e5e7eb;       /* gray-200 */
  --muted: #9ca3af;      /* gray-400 */
  --accent: #2563eb;     /* blue-600 */
  --sun: #ef4444;        /* red-500 */
  --sat: #60a5fa;        /* blue-400 */
  --today: #22c55e;      /* green-500 */
  --border: #374151;     /* gray-700 */
}

* { box-sizing: border-box; }

html, body {
  margin: 0;
  padding: 0;
  background: var(--bg);
  color: var(--text);
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Noto Sans JP", "Hiragino Kaku Gothic ProN", "Meiryo", "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", sans-serif;
}

.container {
  max-width: 900px;
  margin: 40px auto;
  padding: 24px;
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: 16px;
  box-shadow: 0 10px 30px rgba(0,0,0,.25);
}

h1 {
  margin: 0 0 8px;
  font-size: 24px;
  letter-spacing: .02em;
}
h2 {
  margin: 12px 0 16px;
  color: var(--muted);
  font-weight: 600;
}

.controls {
  display: flex;
  gap: 12px;
  align-items: center;
  flex-wrap: wrap;
  margin-top: 12px;
}
.controls label {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-size: 14px;
  color: var(--text);
  background: #0b1220;
  border: 1px solid var(--border);
  padding: 8px 12px;
  border-radius: 10px;
}
.controls input[type="number"] {
  width: 110px;
  padding: 6px 8px;
  border-radius: 8px;
  border: 1px solid var(--border);
  background: #0b1220;
  color: var(--text);
}
.controls button {
  padding: 10px 14px;
  border-radius: 10px;
  border: 1px solid var(--accent);
  background: var(--accent);
  color: white;
  cursor: pointer;
  font-weight: 600;
}
.controls button:hover {
  filter: brightness(1.05);
}

.calendar {
  width: 100%;
  border-collapse: collapse;
  background: #0b1220;
  border: 1px solid var(--border);
  border-radius: 12px;
  overflow: hidden;
}
.calendar th, .calendar td {
  border: 1px solid var(--border);
  text-align: center;
  padding: 10px 6px;
  vertical-align: top;
  height: 86px;
}
.calendar thead th {
  background: #0d1324;
  font-weight: 700;
  letter-spacing: .08em;
}
.calendar th.sun { color: var(--sun); }
.calendar th.sat { color: var(--sat); }

.calendar td.sun { color: var(--sun); }
.calendar td.sat { color: var(--sat); }
.calendar td.today { outline: 2px solid var(--today); outline-offset: -2px; }

.calendar td.empty {
  background: #0a0f1d;
}

.day-num {
  font-size: 18px;
  font-weight: 700;
  margin-bottom: 6px;
}
.ymd {
  font-size: 12px;
  color: var(--muted);
}

.note {
  color: var(--muted);
  margin-top: 14px;
  font-size: 13px;
}